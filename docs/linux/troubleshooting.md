# linux 下问题排查

## 大日志

在程序运行过程中，会遇到非常大的日志，通过正常的命令进行查看日志很难定位日志。

可用一下命令快速找到所需的日志

```shell
# 根据指定关键字，并输出新的文件
cat json.log | grep  -A 100 -B 100 'xxxx' 

# 筛选指定时间范围的日志，并输出新的文件
sed -n '/2024-06-20 10:32:00/,/2024-06-20 10:34:00/p' json.log > test.log

```

